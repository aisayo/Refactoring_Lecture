<%= render partial: "users/errors", locals: {object: @coffee_order} %>

<h1>Edit an existing coffee order:</h1>
<%= form_with model: @coffee_order do |f| %>
  <%= render partial: "form", locals: {in_partial: f} %>

   <%= f.submit %>

<% end %>